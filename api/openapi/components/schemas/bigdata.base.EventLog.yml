allOf:
  - $ref: ./bigdata.base.Event.yml
  - type: object
    properties:
      entity:
        $ref: ./platform.base.Value.yml
      collection:
        type: string
      logGroupId:
        type: integer
        format: uint64
        minimum: 0
      parentId:
        type: integer
        format: uint64
        minimum: 0
      workspaceId:
        type: integer
        format: uint64
        minimum: 0
