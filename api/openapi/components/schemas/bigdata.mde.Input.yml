allOf:
  - $ref: ./bigdata.mde.Operation.yml
  - type: object
    description: Reference on a contextual input.
    properties:
      index:
        type: integer
        format: uint32
        description: Index of the input in the context.
        default: 0
        minimum: 0
