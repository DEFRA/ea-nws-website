allOf:
  - $ref: ./bigdata.relational.SingleTableOperation.yml
  - type: object
    description: |-
      Generate keys from given input data.

      The purpose intermediary class is to identify the operations meant to be
      used for key generation, especially in the context of map-reduce.
