type: object
description: |-
  Store the quantile ratio

  The k-th q-quantile
required:
  - k
properties:
  k:
    type: integer
    format: uint32
    minimum: 1
  q:
    type: integer
    format: uint32
    default: 100
    minimum: 2
