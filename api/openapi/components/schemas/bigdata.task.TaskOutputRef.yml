allOf:
  - $ref: ./bigdata.ref.Ref.yml
  - type: object
    description: Reference to a column in the task output.
    required:
      - name
    properties:
      name:
        type: string
        description: name of the column.
