allOf:
  - $ref: ./community.event.ContractEvent.yml
  - type: object
    required:
      - groupId
    properties:
      groupId:
        type: string
        description: The group id with which the contract performed the operation.
