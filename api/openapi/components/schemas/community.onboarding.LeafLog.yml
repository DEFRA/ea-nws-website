allOf:
  - $ref: ./community.onboarding.Log.yml
  - type: object
    description: |-
      Onboarding operation log received by a Leaf.

      When recieving this event, otherEntity and otherCollection are the ones of
      the node.
