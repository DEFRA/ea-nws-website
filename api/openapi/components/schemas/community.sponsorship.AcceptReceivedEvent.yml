allOf:
  - $ref: ./community.sponsorship.SponsorOfferEvent.yml
  - type: object
    description: |-
      Event sent to the sponsor when the guest accepts the invitation.

      This event is sent after a successfull call to
      one.priv.sponsorship.clear with the status ACCEPTED.
