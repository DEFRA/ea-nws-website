allOf:
  - $ref: ./community.sponsorship.GuestOfferEvent.yml
  - type: object
    description: |-
      Event sent to the guest when he accepts the invitation.

      This event is sent after a successful call to
      one.priv.sponsorship.clear with the status ACCEPTED.
