allOf:
  - $ref: ./community.sponsorship.SponsorOfferEvent.yml
  - type: object
    description: |-
      Event sent to the sponsor when the guest declines the invitation.

      This event is sent after a successful call to
      community.sdk.Sponsor.decline.
