type: object
required:
  - total
properties:
  locations:
    type: array
    description: |-
      the list of locations matching the list
                                options
    items:
      $ref: ./geosafeea.contact.Poi.yml
  total:
    type: integer
    format: int32
    description: |-
      the total number of locations matching the
                                list of options excluding offset and limit
    minimum: 0
