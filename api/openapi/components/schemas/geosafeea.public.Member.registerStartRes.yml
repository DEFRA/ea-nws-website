type: object
required:
  - registerToken
properties:
  registerToken:
    type: string
    description: |-
      a secured token that identifies the
                                 registration session. This token must then
                                 be used in the \ref registerValidate RPC to
                                 finalize the registration procedure.
example: '{"registerToken":"XXXXXXXXXX"}'
