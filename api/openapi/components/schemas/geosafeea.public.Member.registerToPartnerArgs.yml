type: object
required:
  - authToken
  - partnerId
  - params
properties:
  authToken:
    type: string
    description: |-
      a secured token retrieved during
                                 registration or signin
  partnerId:
    type: string
    description: id of the partner
  params:
    description: parameters of the new registration
    allOf:
      - $ref: ./geosafeea.public.RegistrationParams.yml
