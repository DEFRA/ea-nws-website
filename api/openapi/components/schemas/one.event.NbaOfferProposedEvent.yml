allOf:
  - $ref: ./one.event.NbaEvent.yml
  - type: object
    required:
      - proposedCount
    properties:
      proposedCount:
        type: integer
        format: int32
        description: Count of proposed offers this event represents, should always be 1
        minimum: 0
        maximum: 255
