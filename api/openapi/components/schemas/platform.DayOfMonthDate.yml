allOf:
  - $ref: ./platform.MonthDate.yml
  - type: object
    description: A date with a day granularity, defined in the context of a month.
    required:
      - day
    properties:
      day:
        type: integer
        format: uint32
        minimum: 1
        maximum: 31
