allOf:
  - $ref: ./platform.YearDate.yml
  - type: object
    description: A date with a week granularity.
    required:
      - week
    properties:
      week:
        type: integer
        format: uint32
        minimum: 1
        maximum: 53
