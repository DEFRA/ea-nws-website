allOf:
  - $ref: ./platform.base.Date.yml
  - type: object
    description: A date with year granularity.
    required:
      - year
    properties:
      year:
        type: integer
        format: uint32
        minimum: 1900
        maximum: 2100
