allOf:
  - $ref: ./platform.base.CustomValue.yml
  - type: object
    required:
      - epoch
      - hostid
    properties:
      epoch:
        type: integer
        format: int64
        minimum: 0
      hostid:
        type: integer
        format: int32
        minimum: 0
        maximum: 255
