allOf:
  - $ref: ./bigdata.ref.Ref.yml
  - type: object
    description: Reference to the time at which an entity did exit the scenario.
    properties:
      scenario:
        description: |-
          Id of the scenario.

          If not set, will use the scenario in which the reference is used.
        allOf:
          - $ref: ./platform.Id.yml
