allOf:
  - $ref: ./bigdata.base.Action.yml
  - type: object
    description: Base class for action that can be emitted by a scenario.
    properties:
      scenarioId:
        type: integer
        format: int64
        minimum: 0
      ruleId:
        type: integer
        format: int64
        minimum: 0
