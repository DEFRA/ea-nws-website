allOf:
  - $ref: ./platform.base.CustomValue.yml
  - type: object
    description: Store last workspaces visited.
    properties:
      lasts:
        type: array
        items:
          type: integer
          format: uint64
          minimum: 0
