const GENERIC_ERROR_MSG = 'The system encountered an unexpected error'

const GENERIC_OTP_ERROR_MSG =
  'Code not recognised - try again or request a new code'

const EMAIL_IN_USE_ERROR_MSG =
  "You cannot enter this email address as it's already in use - try a different email address"

module.exports = {
  GENERIC_ERROR_MSG,
  GENERIC_OTP_ERROR_MSG,
  EMAIL_IN_USE_ERROR_MSG
}
