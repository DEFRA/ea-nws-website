const urlInfo = '/info'

// Info urls
const infoUrls = {
  floodTypes: urlInfo + '/flood-types',
  floodAreas: urlInfo + '/flood-areas'
}

// Info routes
const infoRoutes = []

export { infoRoutes, infoUrls }
