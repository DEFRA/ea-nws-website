apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ea-nws-website.fullname" . }}-test-connection"
  labels:
    {{- include "ea-nws-website.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ea-nws-website.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
