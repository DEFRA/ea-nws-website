apiVersion: v1
kind: Service
metadata:
  name: {{ include "ea_nws_website.fullname" . }}
  labels:
    {{- include "ea_nws_website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ea_nws_website.selectorLabels" . | nindent 4 }}
