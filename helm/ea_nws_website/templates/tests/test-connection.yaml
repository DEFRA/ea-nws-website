apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ea_nws_website.fullname" . }}-test-connection"
  labels:
    {{- include "ea_nws_website.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ea_nws_website.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
